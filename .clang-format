---
# Canonical style: Google. Kept as BasedOnStyle (not a full --dump-config)
# so the file stays readable across clang-format versions — CI pins
# clang-format-18, but newer versions parse this without erroring on keys
# they don't recognize. Reformat with: clang-format-18 -i.
Language: Cpp
BasedOnStyle: Google
...
